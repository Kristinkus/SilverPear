DB_USERNAME=root_example
DB_PASSWORD=your_mysql_root_password
# Случайная строка для подписи JWT (HS256). Пример: node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
JWT_SECRET=

# С хоста к БД в Docker: localhost:3307 (внутри Compose приложение ходит на db:3306).
